<h1>Results</h1>

{{#each artists}}

<div>
    <h3>{{this.name}}</h3>
    <img class="artist-img" src="{{this.images.0.url}}">
    <a href="/artist-albums/{{this.id}}">
        View Albums
    </a>
</div>

{{/each}}